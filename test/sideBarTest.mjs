describe("Sidebar", () => {
  it("is displayed on all relevant Pages for loggeed in users", () => {});
  it("is displayed not for not logged in users", () => {});
  it("contains all the required elements", () => {});
  it("elements are working correctly", () => {});
});
